package llc.lookatwhataicando.notifai.ui.theme

import androidx.compose.ui.graphics.Color

// Brand — primary blue-indigo family
val Brand80        = Color(0xFFADC6FF)  // light mode containers / dark mode primary
val Brand40        = Color(0xFF1A5CCC)  // dark mode containers / light mode primary

// Secondary — slate/cool grey
val Slate80        = Color(0xFFBBC4D6)
val Slate40        = Color(0xFF4A5568)

// Accent — amber for advisory/warning signals
val Amber80        = Color(0xFFFFDDB3)
val Amber40        = Color(0xFFB36200)

// Neutrals — used for surfaces, backgrounds
val Neutral10      = Color(0xFF1A1C1E)  // dark background
val Neutral20      = Color(0xFF2D2F33)  // dark surface
val Neutral90      = Color(0xFFE2E4E9)  // light surface
val Neutral99      = Color(0xFFFBFCFF)  // light background
